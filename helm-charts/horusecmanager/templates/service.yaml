apiVersion: v1
kind: Service
metadata:
  name: {{ include "horusecmanager.fullname" . }}
  labels:
    {{- include "horusecmanager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "horusecmanager.selectorLabels" . | nindent 4 }}
