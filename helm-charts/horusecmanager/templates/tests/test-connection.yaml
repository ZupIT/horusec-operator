apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "horusecmanager.fullname" . }}-test-connection"
  labels:
    {{- include "horusecmanager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "horusecmanager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
