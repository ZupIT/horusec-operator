{{- if .Values.components.auth.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: horusec-auth
  name: {{ .Values.components.auth.name }}
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "deployment.apiVersion" . }}
    kind: Deployment
    name: {{ .Values.components.auth.name }}
  minReplicas: {{ .Values.components.auth.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.components.auth.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.components.auth.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.components.auth.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.components.auth.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.components.auth.autoscaling.targetMemory }}
    {{- end }}
{{- end }}
